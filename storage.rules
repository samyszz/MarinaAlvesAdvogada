rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for articles images
    match /articles/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Deny all other access by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

